import 'package:flutter/material.dart';

class Divider0 extends StatelessWidget {
  const Divider0({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Divider(height: 0.0);
  }
}
